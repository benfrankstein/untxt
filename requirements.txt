# Core dependencies for Qwen2.5-VL-3B processing
Pillow>=10.0.0
pdf2image>=1.16.0
numpy>=1.24.0
torch>=2.0.0
psutil>=5.9.0

# HuggingFace and model dependencies
huggingface_hub>=0.19.0
transformers>=4.37.0
accelerate>=0.25.0

# vLLM for inference (requires CUDA for best performance)
vllm>=0.3.0

# Qwen VL utilities
qwen-vl-utils

# Optional: DocTR for smart cropping
python-doctr[torch]>=0.7.0

# Optional: GPU monitoring
gputil>=1.4.0
